{
    "version":2,
    "builds": [
      { "src": "*.js", "use": "@vercel/node" }
    ],
    "routes": [
        "src": "/(.*)",
        "dest": "/server.js",
        "methods": ["GET", "POST", "DELETE", "PATCH"],
    "headers": {
        "Access-Control-Allow-Origin": "true"
    }
    ]
}
